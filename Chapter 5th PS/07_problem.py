s = {8, 7, 12, "Harry", [1, 2]}

# We cant change the list inside set as list cant be a element as sets are im-mutable and lists are mutable (tuble are lists which are im-mutable) neither we can select items in a set as there is no indexing
